<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abousalem.messengerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".view.latest_messages.LatestMessagesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.abousalem.messengerapp.view.register.RegisterActivity" />
        <activity android:name="com.example.abousalem.messengerapp.view.login.LoginActivity" />
        <activity android:name=".view.new_message.NewMessageActivity"/>
        <activity android:name=".view.chat_log.ChatLogActivity"/>
        <activity android:name=".base.BaseActivity"/>
    </application>

</manifest>